const WET_PROTOCOL_VERSION = 1;

window.wet.registerExtension({
    applyLanguage: (language) => {
        // console.log(language);
    },
    id: "foobar@2k.com",
    languages: [],
    mainLocale: "en",
    name: "Foobar 2000",
    protocolVersion: WET_PROTOCOL_VERSION,
});
